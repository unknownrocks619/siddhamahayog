<button type="button" data-method='post'
    data-action="{{ route('user.account.event.live', [$program->getKey(), $live->getKey()]) }}"
    class="mt-2 fw-semibold btn btn-sm btn-success live-session-button">
    Join Now
</button>
